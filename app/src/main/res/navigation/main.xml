<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    app:startDestination="@id/createRoom">

    <fragment
        android:id="@+id/createRoom"
        android:name="com.android.tikalarcorefuse.createroom.CreateRoom"
        android:label="CreateRoom" />
    <fragment
        android:id="@+id/roomsList"
        android:name="com.android.tikalarcorefuse.createroom.RoomsListFragment"
        android:label="RoomsList" />
</navigation>